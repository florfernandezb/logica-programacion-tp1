var divisor, 
    dividendo, 
    resto;

divisor = parseInt(prompt("Ingrese el valor del divisor"));
alert("El numero ingresado es: " + divisor);

dividendo = parseInt(prompt("Ingrese el valor del dividendo"));
alert("El numero ingresado es: " + divisor);

resto = divisor % dividendo;
alert("El resto es: " + resto);